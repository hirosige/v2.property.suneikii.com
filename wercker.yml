box: node

# services:
    # - postgres
    # https://devcenter.wercker.com/administration/services/examples/postgresql/

    # - mongo
    # https://devcenter.wercker.com/administration/services/examples/mongodb/
build:
  steps:
    - npm-install
    - npm-test

    - script:
        name: flow-type check
        code: |
          npm run flow

    - script:
        name: eslint check
        code: |
          $(node bin)/eslint pages